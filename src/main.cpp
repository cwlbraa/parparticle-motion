

int main(int argc, char** argv)
{
    //parse input file, numParticles, output file from argv

    //open input file, find dimensions

    //initialize particles based on input dimensions

    while (video not over) { // can we make this run live on a webcam?
        //open evidenceFrame
        //generate emission model : P(evidenceFrame|each location)
        //observe and resample using emissionmodel
        //elapse time : move each particle based on transition model : P(PART'| part)
        //output particles
        //move evidenceFrame pointer
    }

    //free memory
}